name: sellora
description: "Sellora — a subscription marketplace where sellers list CJ Dropshipping-sourced products and buyers shop across the whole catalog."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State management, routing, DI
  get: ^4.6.6

  # Networking — talks only to Sellora's own Cloud Functions, never
  # directly to CJ Dropshipping or IntaSend (see /functions).
  dio: ^5.4.1

  # Backend — Supabase Auth + Postgres (schema and RLS in
  # supabase/migrations). Replaced firebase_auth/cloud_firestore on
  # 2026-09-26; see WORKLOG.md.
  supabase_flutter: ^2.15.4

  # Local session / persistence
  get_storage: ^2.1.1

  # UI / design system
  google_fonts: ^6.2.1
  cached_network_image: ^3.3.1

  # Picking photos on-device — currently only the seller store-branding
  # picker (logo/banner); see lib/core/utils/image_data_url.dart for why
  # picked images are inlined as data: URIs rather than uploaded.
  image_picker: ^1.1.2

  # Sales-over-time chart on the seller dashboard (Phase 9 — analytics).
  fl_chart: ^0.69.0

  # Opening IntaSend's hosted card-checkout page (payOrderCard's
  # checkoutUrl) — card is how non-Kenyan buyers pay (Phase 11).
  url_launcher: ^6.3.2

  # Localization: Material/Cupertino widget strings and locale-aware
  # formatting (Phase 11). flutter_localizations pins intl's exact version.
  flutter_localizations:
    sdk: flutter

  # Utilities
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

# Common next additions, not included to keep this scaffold lean:
#   Supabase Storage   — real hosted uploads for seller photos (product
#                        images, store logo/banner); today those are inlined
#                        as data: URIs instead, see image_data_url.dart
#   connectivity_plus  — offline-state handling

flutter:
  uses-material-design: true

  assets:
    - assets/images/

  # Custom type stack — see lib/app/theme/app_typography.dart for the
  # Google Fonts fallback if you don't bundle these locally.
  # fonts:
  #   - family: Fraunces
  #     fonts:
  #       - asset: assets/fonts/Fraunces-Regular.ttf
  #       - asset: assets/fonts/Fraunces-SemiBold.ttf
  #         weight: 600
